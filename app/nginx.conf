# NGINX configuration file for your web application

# Define the server block
server {
    listen 80;
    server_name example.com;

    # Define location for serving static files
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
